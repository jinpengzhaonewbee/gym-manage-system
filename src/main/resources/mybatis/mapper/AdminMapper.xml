<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.tyut.mapper.AdminMapper">

    <select id="selectByAccountAndPassword" resultType="admin">
        SELECT *
        FROM admin
        WHERE admin_account = #{adminAccount}
          AND admin_password = #{adminPassword}
    </select>
    <select id="findAll" resultType="admin">
        SELECT *
        FROM admin
    </select>
    <insert id="insertAdmin" parameterType="admin">
        INSERT INTO admin (admin_account, admin_password, admin_state, admin_message)
        VALUES (#{adminAccount}, #{adminPassword}, #{adminState}, #{adminMessage})
    </insert>
    <update id="updateAdminByAdminAccount" parameterType="admin">
        UPDATE admin
        SET admin_account       = #{adminAccount},
            admin_password     = #{adminPassword},
            admin_state = #{adminState},
            admin_message   = #{adminMessage}
        WHERE admin_account = #{adminAccount}
    </update>

    <select id="selectByAdminAccount" parameterType="java.lang.Integer" resultType="admin">
        SELECT *
        FROM admin
        WHERE admin_account = #{adminAccount}
    </select>
    <delete id="deleteByAdminAccount" parameterType="java.lang.Integer">
        DELETE
        FROM admin
        WHERE admin_account = #{adminAccount}
    </delete>

</mapper>
